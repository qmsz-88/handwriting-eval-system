# 公网部署指南（临时演示环境）

> 目标：把「中小学生字体书写智能评价系统」部署到公网，让手机浏览器 / PWA / APK 都能访问。

---

## ✅ 当前部署状态（2026-08-21 已上线）

| 项目 | 内容 |
|---|---|
| **公网地址** | **https://soon-tags-game-ancient.trycloudflare.com** |
| 部署方式 | GitHub Actions（`deploy-demo.yml`）+ Cloudflare 快速隧道 |
| 运行状态 | ✅ 全部验证通过（见下方验证清单） |
| 演示窗口 | 约 3 小时（本次运行约 14:38 启动，17:38 左右关闭） |
| 数据说明 | ⚠️ 临时环境：GitHub 运行器文件系统，重启/重新部署会清空注册数据 |

### 验证清单（全部通过 ✅）

| 端点 | 结果 |
|---|---|
| `/api/health` | `{"code":0,"message":"OK"}` |
| `/`（首页双端入口） | 正常显示「中小学生书写智能评价」 |
| `/student.html`（学生端） | 正常 |
| `/parent.html`（家长端） | 正常 |
| `/api/tags`（奖惩标签） | 返回完整 4+4 标签 |
| `/api/materials/3`（三年级素材） | 返回汉字/词语/古诗 |
| `/manifest-student.json`（PWA 清单） | 正常，含图标与 shortcuts |

### 如何重启演示环境（重新生成新地址）

推送任意代码到 `main` 分支即可自动触发 `deploy-demo.yml` 重新部署：
```bash
git push origin main
```
新地址会自动写入仓库根目录 `PUBLIC_URL.txt`。

---

> 说明：下方 Zeabur 方案为**正式长期部署**选项（数据持久、国内访问快），临时演示验证通过后建议迁移。

---

## 为什么选 Zeabur

| 对比项 | Zeabur | Render（国际） | 云服务器 |
|---|---|---|---|
| 费用 | 免费额度，无需买服务器 | 免费层需绑卡 | 约 50~100 元/年 |
| 国内访问速度 | 快（香港/新加坡节点） | 慢、不稳定 | 取决于机房 |
| 部署方式 | GitHub 一键部署，自动构建 | GitHub 自动部署 | 手动执行脚本 |
| 需要维护 | 无 | 无 | 自己管系统/安全 |
| 适合场景 | 试用、演示、小规模家庭使用 | 海外用户 | 正式长期运营 |

---

## 前置条件（已就绪 ✅）

- GitHub 仓库：`https://github.com/qmsz-88/handwriting-eval-system`
- 仓库已包含：`server.js`（Express 后端）、`public/`（完整前端）、`package.json`（含 `start: node server.js`）
- 应用特性：本地 AI 评分算法（不依赖外部 AI 服务）、JSON 文件存储、上传图片本地保存 → **单容器即可运行，无需数据库服务**

---

## 部署步骤（约 5 分钟）

### 第 1 步：注册 Zeabur 账号

1. 浏览器打开 **https://zeabur.com**
2. 点右上角 **Sign up / 注册**，建议直接用 **GitHub 账号登录**（省一次授权）
3. 首次创建项目时需要完成账号验证：**手机号验证 / 预存小额额度 / 绑定信用卡** 三选一，推荐手机号验证（免费）

### 第 2 步：创建项目并导入 GitHub 仓库

1. 登录后点击 **New Project / 新建项目**
2. 进入项目后，点击 **新增服务（Add Service）→ GitHub**
3. 首次使用会弹出 GitHub 授权窗口，点击 **Authorize** 授权 Zeabur
4. 在仓库列表中找到 **handwriting-eval-system**，点击选中
5. 点击 **Deploy / 部署**，等待约 1~3 分钟

> Zeabur 会自动检测到这是 Node.js 项目，自动执行 `npm install` 并运行 `npm start`（即 `node server.js`），**不需要修改任何代码、不需要写配置文件**。

### 第 3 步：绑定免费域名

1. 部署完成后，点击左侧 **网域 / Domains** 分页
2. 点击 **产生域名（Generate Domain）**，选择免费域名如 `xxx.zeabur.app`（可自选前缀）
3. 等待 HTTPS 证书自动签发（约 1 分钟）
4. 域名生效后，直接用手机浏览器打开即可访问系统首页

### 第 4 步：验证系统

1. 打开公网地址 → 进入首页
2. 注册家长账号 → 登录家长端
3. 添加孩子（填写出生年月日、年级等）
4. 用学生端扫码/令牌登录 → 拍照评测 → 查看评分与双报告
5. 家长端查看孩子提交的记录 → 确认数据同步

---

## 重要说明

### 1. 临时演示环境的数据

- Zeabur 免费容器的磁盘是**临时的**：实例重启/重新部署后，注册的用户、评测记录、上传图片会**丢失**
- 适合：先验证系统效果、给家人试用
- ⚠️ 请勿在演示环境录入重要数据

### 2. 免费层限制（2026 现状）

- 每月有免费额度，个人试用足够
- 香港/新加坡节点延迟低，国内访问流畅
- 实例空闲可能休眠，再次访问会自动唤醒（稍等几秒）

### 3. 部署后 APK / PWA 如何连接

- **PWA（手机浏览器「添加到主屏幕」）**：直接用公网地址打开即可，无需任何修改
- **APK（已打包的安卓安装包）**：APK 内前端目前指向相对路径 `/api`，需要改为公网服务器地址后重新打包。部署成功后把公网地址发给我，我来帮你配置并重新构建 APK

---

## 部署完成后的正式方案（可选）

临时演示验证通过后，如果想长期稳定使用（数据不丢失、国内访问快、可绑定自己的域名），推荐二选一：

| 方案 | 成本 | 数据持久化 | 说明 |
|---|---|---|---|
| **Zeabur 付费套餐** | 约 $5/月起 | 支持挂载存储卷 | 零维护，升级即用 |
| **腾讯云/阿里云轻量服务器** | 约 50~100 元/年 | 永久保存 | 我可提供一键部署脚本（systemd + Nginx + HTTPS） |

届时我把部署脚本、数据迁移方法一并准备好。

---

## 遇到问题？

- 部署失败：查看服务页面 **日志（Logs）**，常见原因是网络拉取依赖超时，点击 **Redeploy** 重试即可
- 域名打不开：确认已生成域名并等待证书签发；若仍不行，到「网域」重新生成
- 其他问题：把日志或截图发给我，我帮你排查
